#!/usr/bin/env python

import unittest
import nidhoeggr
from config import config
from socket import socket, AF_INET, SOCK_STREAM

class RacelistServerNetworkTest(unittest.TestCase):

	def test(self):
		self.assertEqual(1,1)

class BroadcastNetworkServerTest(unittest.TestCase):

	def test(self):
		self.assertEqual(1,1)

class ScaryProtocolTest(unittest.TestCase):

	def test(self):
		self.assertEqual(1,1)

if __name__=='__main__':
	unittest.main()
